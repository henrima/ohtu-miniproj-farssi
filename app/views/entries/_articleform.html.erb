<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>

  <div class="field">
    <%= f.label @entry.fields %><br>
    <%= f.text_field @entry.fields %>
  </div>


  <div class="field">
    <%= label_tag @author.name %><br>
    <%= text_field_tag @author.content %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
