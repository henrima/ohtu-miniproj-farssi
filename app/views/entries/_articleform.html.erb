<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>

  <div class="field">
    <%= fields_for(:author) do |a| %>
      <%= a.label "AUTHOR" %><br>
      <%= a.text_field :content %>
    <% end %>
  </div>

  <div class="field">
    <%= fields_for(:title) do |a| %>
      <%= a.label "TITLE" %><br>
      <%= a.text_field :content %>
    <% end %>
  </div>
  <div class="field">
    <%= fields_for(:journal) do |a| %>
      <%= a.label "JOURNAL" %><br>
      <%= a.text_field :content %>
    <% end %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
